import styled from "styled-components";

export const Users = () => {
  return (
    <SContainer>
      <h2>USERS一覧</h2>
    </SContainer>
  );
};

const SContainer = styled.div`
  text-align: center;
`;
